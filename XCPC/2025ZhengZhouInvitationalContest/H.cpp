#include <bits/stdc++.h>
using namespace std;
using i64 = long long;

void solve () {
    int a, l, r;
    cin >> a >> l >> r;
    cout << r - l + 1 << "\n";
}

int main () {
    cin.tie(nullptr), cout.tie(nullptr) -> sync_with_stdio(false);
    int T;
    cin >> T;
    while (T--) {
        solve();
    }
}